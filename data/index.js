const options = require('./actionableOptions.json')
const metadata = require('./metadata.json')
const insights = require('./insights.json')
const events = require('./events.json')

module.exports = {
  options,
  metadata,
  insights,
  events,
}
